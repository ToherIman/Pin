/*
import {Meteor} from 'meteor/meteor';
import {webshot} from 'meteor/bryanmorgan:webshot';
const fs = require('fs');

Meteor.methods({
  'shot' () {
    webshot("http://google.com", "/tmp/google.png", function (err) {
  // screenshot saved to /tmp/google.png
});
  }
});
*/